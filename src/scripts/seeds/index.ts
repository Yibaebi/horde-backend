import generateBudgetData from './budget';
import generateExpenses, { transformCategories } from './expenses';

export const dbSeedDataGenInterface = {
  generateBudgetData,
  generateExpenses,
  transformCategories,
};

export * from './budget';
export * from './budget';
export default dbSeedDataGenInterface;
